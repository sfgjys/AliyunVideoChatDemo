<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical">

        <EditText
            android:id="@+id/et_username"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_gravity="center_vertical"
            android:layout_margin="20dp"
            android:background="@drawable/login_input_bg_rect"
            android:hint="@string/user_name"
            android:maxLines="1"
            android:paddingLeft="30dp"
            android:textColor="@color/blue_7b_normal_color"
            android:textColorHint="@color/row_divider"
            android:textCursorDrawable="@drawable/live_edit_cursor"
            android:textSize="16sp" />

        <Button
            android:id="@+id/iv_login_phone_next"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_gravity="center_vertical"
            android:layout_margin="20dp"
            android:background="@drawable/login_btn_bg_rect"
            android:gravity="center"
            android:maxLines="1"
            android:onClick="onClick"
            android:text="@string/login"
            android:textColor="#fff"
            android:textSize="16sp" />

    </LinearLayout>

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:contentDescription="@string/only_one_image"
        android:src="@drawable/login_bg" />
</LinearLayout>