<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black_transprent_80" >

    <TextView
        android:id="@+id/tv_back"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:textColor="@android:color/white"
        android:textSize="@dimen/font_size_28px"
        android:background="@drawable/bg_live_close_button"
        android:text="@string/back"
        android:gravity="center"
        android:layout_marginBottom="135dp"
        android:layout_marginLeft="65dp"
        android:layout_marginRight="65dp"
        android:layout_alignParentBottom="true" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_above="@id/tv_back"
        android:gravity="center" >

        <TextView
            android:id="@+id/tv_finished"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/live_close_title_color"
            android:textSize="@dimen/font_size_36px"
            android:gravity="center"
            android:text="@string/live_finished" />

    </LinearLayout>

</RelativeLayout>